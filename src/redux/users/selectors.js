export const selectUsers = state => state.usersData.users;
export const selectError = state => state.usersData.error;
export const selectCurrentPage = state => state.usersData.currentPage;

// const { currentPage, allData } = useSelector((state) => state.pagination);
